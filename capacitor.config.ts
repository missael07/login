import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.asistencias.app',
  appName: 'login',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
